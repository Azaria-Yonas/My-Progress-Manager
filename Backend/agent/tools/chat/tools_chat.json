[
    {
        "type": "function",
        "name": "ask_user",
        "description": "Ask the user a question.",
        "parameters": {
            "type": "object", 
            "properties": { 
                "question": { 
                    "type": "string",
                    "description": "The question to be asked to the user." 
                }
            },
            "required": ["question"],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "wakeup_orchestrator",
        "description": "Handoff task to Orchestrator",
        "parameters": {
            "type": "object", 
            "properties": { 
                "chat": { 
                    "type": "string",
                    "description": "Current chat between the agent and the user" 
                }
            },
            "required": ["chat"],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "fetch_tasks",
        "description": "API TOOL: Fetch all tasks",
        "parameters": {
            "type": "object",
            "properties": {},
            "required": [],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "create_task",
        "description": "API TOOL: Create a new task",
        "parameters": {
            "type": "object", 
            "properties": { 
                "title": { 
                    "type": "string",
                    "description": "A short title for the task" 
                }, 
                "color": {
                    "type": "string",
                    "description": "The color of the tasks component given as a hex code" 
                },
                "due_date": { 
                    "type": "string",
                    "description": "The due date of the task in YYYY-MM-DD format" 
                }
            },
            "required": ["title", "color", "due_date"],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "update_task",
        "description": "API TOOL: Update one or more fields of an existing task. Fields left as null are unchanged.",  
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string", 
                    "description": "The id of the task to update"
                }, 
                "title": {
                    "type": ["string", "null"], 
                    "description": "The new title of the task, or null to leave unchanged"
                },
                "color": { 
                    "type": ["string", "null"], 
                    "description": "The new color tag of the task, or null to leave unchanged"
                }, 
                "due_date": {
                    "type": ["string", "null"],
                    "description": "The new due date in YYYY-MM-DD format, or null to leave unchanged"
                }, 
                "order_index": {
                    "type": ["integer", "null"], 
                    "description": "The new sort order index of the task, or null to leave unchanged"
                },   
                "is_completed": {
                    "type": ["boolean", "null"],  
                    "description": "The new completion state of the task, or null to leave unchanged"
                }  
            },
            "required": ["id", "title", "color", "due_date", "order_index", "is_completed"],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "delete_task",
        "description": "API TOOL: Delete task.",
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string",
                    "description": "The id of the task to delete"
                }
            },
            "required": ["id"],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "complete_task",
        "description": "API TOOL: Mark a task as complete, moving it into the user's completed tasks history.",
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string",
                    "description": "The id of the task to complete"
                }
            },
            "required": ["id"],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "undo_complete_task",
        "description": "API TOOL: Restore a previously completed task back into the user's active task list.",
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string",
                    "description": "The id of the completed task to restore"
                }
            },
            "required": ["id"],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "reorder_tasks",
        "description": "API TOOL: Update the sort order of multiple tasks at once.",
        "parameters": {
            "type": "object",
            "properties": {
                "tasks": {
                    "type": "array",
                    "description": "The list of tasks and their new order indexes",
                    "items": {
                        "type": "object",
                        "properties": {
                            "id": {
                                "type": "string",
                                "description": "The id of the task"
                            },
                            "order_index": {
                                "type": "integer",
                                "description": "The new sort order index of the task"
                            }
                        },
                        "required": ["id", "order_index"],
                        "additionalProperties": false
                    }
                }
            },
            "required": ["tasks"],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "fetch_streaks",
        "description": "API TOOL: Fetch all streaks.",
        "parameters": {
            "type": "object",
            "properties": {},
            "required": [],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "create_streak",
        "description": "API TOOL:  Create a new streak",
        "parameters": {
            "type": "object",
            "properties": {
                "title": {
                    "type": "string",
                    "description": "The title of the streak"
                },
                "duration_seconds": {
                    "type": "integer",
                    "description": "The length of each streak interval, in seconds"
                }
            },
            "required": ["title", "duration_seconds"],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "update_streak",
        "description": "API TOOL: Update one or more fields of an existing streak. Fields left as null are not changed.",
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string",
                    "description": "The id of the streak to update"
                },
                "title": {
                    "type": ["string", "null"],
                    "description": "The new title of the streak, or null to leave unchanged"
                },
                "duration_seconds": {
                    "type": ["integer", "null"],
                    "description": "The new interval length in seconds, or null to leave unchanged"
                },
                "cooldown_end": {
                    "type": ["string", "null"],
                    "description": "The new cooldown end timestamp in ISO 8601 format, or null to leave unchanged"
                },
                "streak_count": {
                    "type": ["integer", "null"],
                    "description": "The new streak count, or null to leave unchanged"
                },
                "last_tap_at": {
                    "type": ["string", "null"],
                    "description": "The new last tap timestamp in ISO 8601 format, or null to leave unchanged"
                },
                "paused": {
                    "type": ["boolean", "null"],
                    "description": "The new paused state, or null to leave unchanged"
                }
            },
            "required": ["id", "title", "duration_seconds", "cooldown_end", "streak_count", "last_tap_at", "paused"],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "delete_streak",
        "description": "API TOOL: Permanently delete a streak.",
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string",
                    "description": "The id of the streak to delete"
                }
            },
            "required": ["id"],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "tap_streak",
        "description": "API TOOL: Increment a streak's count, resetting its cooldown period.",
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string",
                    "description": "The id of the streak to tap"
                }
            },
            "required": ["id"],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "expire_streak",
        "description": "API TOOL: Reset a streak's count and cooldown after it has expired.",
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string",
                    "description": "The id of the streak to expire"
                }
            },
            "required": ["id"],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "pause_streak",
        "description": "API TOOL: Pause or unpause a streak.",
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string",
                    "description": "The id of the streak to pause or unpause"
                },
                "paused": {
                    "type": "boolean",
                    "description": "True to pause the streak, false to unpause it"
                }
            },
            "required": ["id", "paused"],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "complete_streak",
        "description": "Mark a streak as complete, moving it into the user's completed streaks history along with its interval and calendar data.",
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string",
                    "description": "The id of the streak to complete"
                },
                "total_intervals": {
                    "type": "integer",
                    "description": "The total number of intervals attempted for this streak"
                },
                "successful_intervals": {
                    "type": "integer",
                    "description": "The number of intervals successfully completed"
                },
                "failed_intervals": {
                    "type": "integer",
                    "description": "The number of intervals that were failed or missed"
                },
                "calendar_data": {
                    "type": "object",
                    "description": "A map of date strings to interval outcome objects, each with a status and index",
                    "additionalProperties": true
                }
            },
            "required": ["id", "total_intervals", "successful_intervals", "failed_intervals", "calendar_data"],
            "additionalProperties": false
        },
        "strict": false
    },
    {
        "type": "function",
        "name": "get_tasks_analytics",
        "description": "Fetch analytics data for the authenticated user's completed tasks. And give meaningful insight.",
        "parameters": {
            "type": "object",
            "properties": {},
            "required": [],
            "additionalProperties": false
        },
        "strict": true
    },
    {
        "type": "function",
        "name": "get_streaks_analytics",
        "description": "Fetch analytics data for the authenticated user's completed streaks. And give meaningful insight.",
        "parameters": {
            "type": "object",
            "properties": {},
            "required": [],
            "additionalProperties": false
        },
        "strict": true
    }

]

