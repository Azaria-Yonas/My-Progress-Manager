


the brains of any agent is the llm while the tools we code around it are its arm and legs.



It is what decides everything and calls every tool.




However, they don't come cheap and need to be utilized wisely.

we need to be wise in how the agent is fed data. and how the context window is managed.

for instance the bootstrap files dont need to be injected for summary tasks (that why i created a summary agent)


I also implement a function to mathematically minimize token consuption